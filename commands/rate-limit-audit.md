---
description: Lista endpoints sin rate limiting detectado. Next.js, Express, Laravel.
argument-hint: "[path]"
---

Corre `~/.claude/scripts/rate-limit-audit.sh $ARGUMENTS`. Los endpoints marcados ❌ son candidatos a issue `[SEC-NNN]`. No crear issues automáticamente — preguntar si delegamos al `issue-manager`.
