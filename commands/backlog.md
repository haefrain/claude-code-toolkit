---
description: Backlog compacto for:claude del repo actual (o especificado).
argument-hint: "[owner/repo opcional]"
---

Ejecuta `~/.claude/scripts/gh-backlog.sh $ARGUMENTS` y muestra la salida tal cual. No enriquezcas, no leas bodies, no propongas acciones. Solo devuelve el output del script.
