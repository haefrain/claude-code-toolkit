---
description: Resumen compacto de un PR — descripción, archivos tocados, comentarios sin resolver, reviews.
argument-hint: "[pr-number] [owner/repo]"
---

Corre `~/.claude/scripts/pr-context.sh $ARGUMENTS`. Si no se especifica número, usa el PR de la rama actual. Mostrar el resultado completo. Si hay comentarios sin resolver, mencionarlos como próxima acción.
