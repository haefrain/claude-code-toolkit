---
description: Estado compacto de los últimos GitHub Actions runs. Tabla de status/conclusión por rama.
argument-hint: "[owner/repo]"
---

Corre `~/.claude/scripts/ci-status.sh $ARGUMENTS` y muestra el resultado. Si hay fallos, sugerir usar `/failing-tests` para ver el detalle.
