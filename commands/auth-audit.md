---
description: Lista endpoints y páginas con/sin protección de auth. Next.js, Laravel.
argument-hint: "[path]"
---

Corre `~/.claude/scripts/auth-routes-audit.sh $ARGUMENTS`. Los marcados ❌ son candidatos a revisión. Recordar que la ausencia de `auth()` en un route handler Next.js puede ser intencional (endpoints públicos). Preguntar antes de crear issues.
