---
description: Resumen del schema de DB: modelos, campos PII, enums, migrations. Para Prisma, Drizzle y Laravel.
argument-hint: "[path]"
---

Corre `~/.claude/scripts/db-schema.sh $ARGUMENTS` y muestra el resultado. Los campos marcados ⚠️PII sin 🔒 son candidatos a revisar con `/pii-in-prisma`. No leer el schema.prisma completo — usar este resumen como referencia.
